# Hyphens Rule

Controls the number of spaces after hyphens (`-`).

## Options

- `max-spaces-after`: Maximum spaces allowed after hyphens

## Default Configuration

```yaml
rules:
  hyphens:
    max-spaces-after: 1
```

## Special Configuration

This rule is enabled by default with strict spacing requirements.
