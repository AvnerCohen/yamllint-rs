# Line Length Rule

## Description
Use this rule to set a maximum line length.

## Options
- `max`: Maximum line length (default: 80)
- `level`: Error level (error or warning)

## Default Configuration
```yaml
rules:
  line-length:
    max: 80
    level: error
```
