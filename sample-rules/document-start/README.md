# Document Start Rule

Requires document start marker (`---`) at the beginning of files.

## Options

No specific options - rule is either enabled or disabled.

## Default Configuration

```yaml
rules:
  document-start: {}
```

## Special Configuration

This rule is enabled by default.
